/**
 * Sample Skeleton for 'hello-view.fxml' Controller Class
 */

package com;

import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.control.ListView;
import javafx.scene.control.Tab;

public class HelloController {

    @FXML // fx:id="payButton"
    private Button payButton; // Value injected by FXMLLoader

    @FXML // fx:id="myListView"
    private ListView<?> myListView; // Value injected by FXMLLoader

    @FXML // fx:id="buttton2"
    private Button buttton2; // Value injected by FXMLLoader

    @FXML // fx:id="idTAG"
    private ListView<?> idTAG; // Value injected by FXMLLoader

    @FXML // fx:id="welcomeText"
    private Label welcomeText; // Value injected by FXMLLoader

    @FXML // fx:id="addButton"
    private Button addButton; // Value injected by FXMLLoader

    @FXML // fx:id="nameTag"
    private Label nameTag; // Value injected by FXMLLoader

    @FXML // fx:id="owedTag"
    private Label owedTag; // Value injected by FXMLLoader

    @FXML // fx:id="roleTag"
    private Label roleTag; // Value injected by FXMLLoader

    @FXML // fx:id="removeButton"
    private Button removeButton; // Value injected by FXMLLoader

    @FXML // fx:id="employeesTab"
    private Tab employeesTab; // Value injected by FXMLLoader

    @FXML // fx:id="addOwedButton"
    private Button addOwedButton; // Value injected by FXMLLoader

    @FXML // fx:id="button1"
    private Button button1; // Value injected by FXMLLoader

}
